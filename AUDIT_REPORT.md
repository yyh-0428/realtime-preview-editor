# Werkstatt CDN 回退与功能整合审计

日期：2026-07-26

## 结论

手机版演示文件确实不是建立在上一轮“全面修复版”上，而是套用了较早的项目基线。因此 CDN 库面板、旧模式逻辑和若干已修复缺陷被重新带回。这个现象不是浏览器缓存，也不是用户操作错误。

本次已把手机版适配重新合并到最新功能链，并逐项恢复此前改动。

## 根因

1. 手机版补丁的输入文件来自旧基线，而非最新综合修复版。
2. 旧基线仍包含 CDN 面板、旧 Agent/AI 状态、旧提示词及旧安全逻辑。
3. 在界面删除 CDN 面板后，初始化流程仍残留 `initCdnPanel()` 调用；页面运行到这里会抛出 `ReferenceError`，后续初始化可能中断。
4. 因为移动端修改与功能修改不是在同一基线上合并，最终交付出现“手机版正确、核心功能回退”的版本分叉。

## 已恢复的功能

### 依赖管理

- 删除 CDN 标签、搜索框、预设库和用户管理入口。
- 删除残留 `initCdnPanel()` 调用。
- Agent 仅能通过 `enable_dependency(library)` 请求可信内置库名。
- 依赖地址由内部 HTTPS 白名单解析、去重并插入。
- 普通用户界面不再出现“CDN”字样。

### AI / Agent 结构

- 只保留 AI 模式和 Agent 模式两个主入口。
- 深度思考恢复为 AI 模式内部开关。
- 切换 Agent 时隐藏深度思考设置；切回 AI 后保留原状态。
- 深度思考保留最近对话历史。
- Agent 尊重用户当前选择的模型。

### 项目规范

- “CTX”统一显示为“项目规范”。
- 优先级为：本次明确要求 > PRODUCT.md / DESIGN.md > 当前页面风格 > 视觉模板 > 通用默认。
- 普通 AI 与 Agent 都会获得同一套项目规范。
- 项目规范生成入口正确 `await` Agent，不再刚启动就被当作中断。
- 未定义项目规范时，不再错误要求普通 AI 调用一个不存在的保存工具。

### Agent 工具协议

- MCP 工具定义转换为 OpenAI-compatible function schema。
- 支持 `tool_calls`、`toolCalls`、旧版 `function_call` 和 Anthropic `tool_use`。
- 支持流式工具名/参数分片与双层 JSON 参数。
- 空工具名会切换兼容协议，不再伪造 `unknown_tool`。
- 未注册工具会为同一批每个 `tool_call_id` 返回对应错误结果。
- `complete` 必须单独调用，且只能在修改与验证结束后调用。
- 修改后必须通过 `run_code_check`；UI任务还需通过 `visual_verify`。
- 20轮/40次工具预算耗尽后显示“未完成”，不会覆盖成“完成”。
- 相同工具与相同参数超过两次会被拦截。
- 降级协议不再重复写入同一 assistant 消息，也不会无工具时自动应用代码或宣称完成。

### 安全与隐私

- 预览 iframe 始终保留 `sandbox="allow-scripts allow-popups"`。
- 预览消息只接受目标 iframe 的 `event.source`。
- AI Markdown/HTML 经过 DOMPurify；库不可用时使用本地白名单净化。
- 公网 API/MCP 地址必须使用 HTTPS；localhost 可使用 HTTP。
- URL 中禁止账号、密码、Token、API Key 等敏感查询参数。
- 网络请求不携带浏览器 Cookie。
- AI API Key 和 MCP Key 只保存在 sessionStorage。
- 外部 MCP 工具默认只开放明确只读能力。

### 提示词与能力声明

- 只声明真实支持的 GSAP Core 与 ScrollTrigger。
- 不再暗示 SplitText、MorphSVG、DrawSVG、ScrollSmoother 已安装。
- 公共 API 提示词从虚假的“80+ 内置 API”改为当前真实维护的8个 HTTPS 示例。
- 普通 AI 对超过160,000字符的文件不再整份发送。
- Agent 对大文件使用关键词或最多500行的分段分析。
- 格式化恢复为 Prettier，不使用破坏代码的简易格式器。

### 手机版

- 编辑、分屏、预览三个主视图与 AI 抽屉入口保持一致。
- 底部导航状态与真实布局同步。
- iPhone 刘海/Home 指示器安全区、动态地址栏和软键盘已处理。
- 输入控件在 iOS 下保持16px，避免聚焦自动缩放。
- 主要触控目标至少44px。
- 竖屏分屏比例独立保存；横屏手机有独立布局逻辑。
- 模态框支持 Escape、Tab 焦点循环和焦点恢复。
- 补充所有表单控件的可访问名称。

## 验证结果

- 综合集成审计：61/61 通过。
- 旧功能静态回归：22/22 通过。
- Agent 工具协议测试：通过。
- MCP 路由与会话密钥测试：通过，共模拟5次 RPC。
- 所有内联及本地 JavaScript 语法检查：通过。
- 重复 HTML ID：0。
- 缺少 `type="button"` 的静态按钮：0。
- 未命名表单控件：0。
- 无效 ARIA 引用：0。
- 未定义 CSS 变量：0。
- 缺失初始化函数：0。
- 缺失本地依赖文件：0。
- 本地 HTTP 访问：返回 200 OK。

## 尚不能替代的验证

当前容器中的 Chromium 因 DBus/zygote 环境异常在20秒后超时，未能完成真实页面渲染和截图。因此没有声称“浏览器视觉实测通过”。此外，真实模型 API、图片模型和你的 MCP 服务器需要在你的实际服务环境中做端到端验证。
