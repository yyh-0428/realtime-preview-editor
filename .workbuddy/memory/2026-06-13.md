# 2026-06-13 工作日志

## Werkstatt 实时预览编辑器 — 专家审查汇总

### 完成的工作
1. **竞品差距分析**（discovery-analyst）：产出 `ai-competitor-analysis.md`，明确 Werkstatt 与 Cursor/Bolt/v0 等竞品的 6 大能力差距
2. **五维设计质量评审**（critique-reviewer）：产出 `critique-review-report.md`，评分 3/3/3/3/2，核心结论为"交互精致、架构贪婪"
3. **代码质量审查**（code-auditor）：产出 `code-quality-audit.md`，发现 34 个问题（3 Critical + 8 High + 16 Medium + 7 Low）
4. **综合改进路线图**：产出 `werkstatt-redesign-roadmap.md`，按 P0/P1/P2 分级，含具体时间线和成功指标

### 关键发现
- 单文件膨胀至 260KB/4601 行，是最根本的架构问题
- 2 个 Critical 安全漏洞：iframe XSS 注入面、API Key 明文存储
- undo/redo 完全缺失，AI 修改无撤销能力
- GSAP "全插件内置"是虚假承诺，iframe 未真正加载 GSAP
- AI 仍是单次调用，无 ReAct/工具调用能力

### 三大 P0 修正
1. 数据外置（设计系统/知识库拆为 JSON 按需加载）→ 目标 <120KB
2. 补齐 undo + 前端验证 + iframe 错误捕获
3. 修复 2 个 Critical 安全漏洞

### 增量编辑 Search/Replace 修复（第一轮）
- **问题**：AI 返回的 SEARCH 部分包含过多上下文（甚至整个函数），导致匹配失败或替换范围过大
- **修复 1（提示词）**：`outputFormatRule` 中明确限制 SEARCH 最多 15 行，添加具体示例展示"最小上下文"用法
- **修复 2（匹配逻辑）**：`findUniqueMatch` 增加第三级"忽略每行前导空白"的容错匹配，处理 AI 缩进差异
- **文件位置**：`/Users/hanhan/WorkBuddy/2026-06-13-09-07-13/realtime-preview-editor-redesign.html`

### 增量编辑 Search/Replace 修复（第二轮）
- **问题 A（用户反馈）**：AI 输出 `<<< SEARCH` 内容后，编辑器没有正确执行替换，而是把包含标记的原始文本直接覆盖到编辑器里
- **根因**：`addDiffCard` 的"应用到编辑器"按钮直接执行 `setCode(code)`，其中 `code` 是包含 `<<< SEARCH` / `>>> REPLACE` 标记的原始 AI 输出
- **修复 A**：按钮点击时先调用 `parseSearchReplaceBlocks` + `applySearchReplaceBlocks` 尝试正确应用；若仍失败，提示用户并允许手动确认后插入 REPLACE 部分
- **问题 B**：`parseSearchReplaceBlocks` 使用正则 `/<<<!SEARCH[\s\S]*?>>>REPLACE/`，若 SEARCH 内容中包含 `>>>` 字符会提前误匹配
- **修复 B**：将正则解析改为基于 `indexOf` 的精确索引解析，彻底避免内容中的特殊字符干扰
- **文件位置**：`/Users/hanhan/WorkBuddy/2026-06-13-09-07-13/realtime-preview-editor-redesign.html`

### 增量编辑系统重构——基于行号的精准编辑（第三轮）
- **用户反馈**：Search/Replace 文本匹配不可靠，AI 应直接输出基于行号的编辑指令
- **重构方案**：
  1. **提示词中代码带行号**：`buildAiPayload` 中给 `currentCode` 每行加上 `  1 | ` 前缀，让 AI 知道每行的精确行号
  2. **新的 AI 输出格式**：`REPLACE 5-8:` / `INSERT after 20:` / `DELETE 30-32`，每个块以 `END` 结束，包裹在 ````edit` 代码块中
  3. **新的解析函数**：`parseEditBlocks` 解析行号操作，`applyEditBlocks` 根据行号直接用 `editor.replaceRange` 操作（无文本匹配）
  4. **三层回退策略**：先尝试 `parseEditBlocks` → 再尝试 `parseSearchReplaceBlocks` → 最后回退到完整代码块
  5. **按钮逻辑同步更新**：`addDiffCard` 的"应用到编辑器"按钮同样先尝试行号编辑
- **删除的硬性限制**：取消了"SEARCH 最多 15 行"等限制，改为基于行号的自由编辑
- **文件位置**：`/Users/hanhan/WorkBuddy/2026-06-13-09-07-13/realtime-preview-editor-redesign.html`

### 文件位置
- `/Users/hanhan/WorkBuddy/2026-06-13-09-07-13/critique-review-report.md`
- `/Users/hanhan/WorkBuddy/2026-06-13-09-07-13/code-quality-audit.md`
- `/Users/hanhan/WorkBuddy/2026-06-13-09-07-13/werkstatt-redesign-roadmap.md`
